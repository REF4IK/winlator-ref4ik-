# cmake files
/build

# cargo files
/ui/target

# ide/lsp files
/.zed
/.vscode
/.clangd
/.cache
/.ccls

build-android/

# CI / issue templates / funding (not needed in fork)
/.github
/.gitmodules
/scripts

# pe-parse is a Windows-only PE parser dependency, not used on Android
/thirdparty/pe-parse

# toml11 tests / tools (not needed for build)
/thirdparty/toml11/tests
/thirdparty/toml11/tools

# volk source library — we use system Vulkan headers, not volk
/thirdparty/volk
